<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.storm.template">

    <dist:module dist:instant="true" />

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".module.sdialog.SdialogActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".module.item.ItemActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".module.viewpage.ViewPagerActivityActivity" />
        <activity android:name=".module.nav.NavActivity" />
        <activity android:name=".module.empty.EmptyActivity" />
        <activity android:name=".module.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>